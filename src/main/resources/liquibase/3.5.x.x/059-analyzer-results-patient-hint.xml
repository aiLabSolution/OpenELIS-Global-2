<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="lis239-001-add-analyzer-results-patient-hint" author="labsolution">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists schemaName="clinlims" tableName="analyzer_results" columnName="patient_hint"/>
            </not>
        </preConditions>
        <comment>Wire patient identity (bridge FHIR Patient identifier, e.g. HL7 PID-2) carried into analyzer staging so a same-day two-patient collision under a shared/reused accession is detectable at the accept boundary (LIS-239). Null = source carried no patient identity.</comment>

        <addColumn schemaName="clinlims" tableName="analyzer_results">
            <column name="patient_hint" type="VARCHAR(80)"/>
        </addColumn>

        <rollback>
            <dropColumn schemaName="clinlims" tableName="analyzer_results" columnName="patient_hint"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
